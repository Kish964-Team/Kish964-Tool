# Kish964 v2 – Python Dependencies
# Install with: pip install -r requirements.txt

aiohttp>=3.9.0       # Async HTTP client
aiodns>=3.2.0        # Async DNS resolver
rich>=13.0.0         # Terminal output (progress bars, tables, colour)
pyfiglet>=1.0.2      # ASCII art banner
mmh3>=4.0.0          # MurmurHash3 for favicon hashing (optional but recommended)
